import { ChristmasTree } from '@/components/ChristmasTree';

const Home = () => {
  return (
    <main className="min-h-screen w-full bg-[#000a1f] flex items-center justify-center overflow-hidden">
      <ChristmasTree />
    </main>
  );
}

export default Home;
